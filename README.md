# template-python-lib
A template Python library.

Setup instructions:
1. Clone the repo under a new name, e.g. with the `Use this template` button in GitHub.
2. Run: `./init`

This project uses `uv` for venv and dependency management. Make sure you have it installed ([instructions](https://docs.astral.sh/uv/getting-started/installation/)).
